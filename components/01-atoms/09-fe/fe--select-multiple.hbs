<select class="form-select" type="select" id="formselect" size="10" multiple>
  <optgroup label="Treffe Deine Wahl">
    {{#each options}}
    <option value="{{ value }}">{{ entry }}</option>
    {{/each}}
  </optgroup>
</select>
